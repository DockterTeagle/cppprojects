#include <string>
#include <vector>
class Solution {
 public:
  int countCharacters(std::vector<std::string> &words, std::string chars) {
    for (std::size_t i = 0; i < words.size(); ++i) {
      for (std::size_t j = 0; j < words.at(i).length(); ++j) {
      }
    }
  }
};
int main() { return 0; }
