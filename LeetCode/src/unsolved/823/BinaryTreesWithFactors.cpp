// #include "BinaryTreesWithFactors.h"
// using std::string, std::vector;
// int Solution::numFactoredBinaryTrees(vector<int> &arr) {
//   std::sort(arr.begin(), arr.end());
//   std::size_t numOfTrees = 0;
//   numOfTrees += arr.size();
//   for (std::size_t i = 0; i < arr.size();
//        ++i) {  // gameplan for each number find how many factors it has and
//                // then see if it exists in the array,gonna have to change the
//                i
//                // size later
//     // fist off check if prime
//     //  (x-1)^p - (x^p-1) if all coefficients are divisble by p its prime
//   }
//   return 0;
// }
